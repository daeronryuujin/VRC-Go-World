# VRC-Go-World

VRchat world for go players.

## Planning

- See `IMPLEMENTATION_PLAN.md` for a split execution plan with:
  - tasks for the world creator,
  - tasks I can do autonomously in-repo,
  - and explicit parallelization guidance.

- Use `KANBAN.md` for ongoing task tracking (Backlog -> Done with dependency + parallel metadata).
- See `docs/video-first-setup.md` for the phase-1 video integration checklist.
- See `AUTONOMOUS_CHANGES.md` for a log of code/assets implemented autonomously.
- Prototype queue code lives in `scripts/Video/` and UI wireframe assets in `assets/ui/`.
